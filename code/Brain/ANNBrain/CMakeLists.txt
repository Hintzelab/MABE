option(enable_Brain_ANN "Enable Module ANNBrain" OFF)
if (enable_Brain_ANN)
  register_module(Brain ANN)
  target_sources(${EXE} PRIVATE ${CMAKE_CURRENT_LIST_DIR}/ANNBrain.cpp)
  target_sources(${EXE} PRIVATE ${CMAKE_CURRENT_LIST_DIR}/ANNBrain.h)
endif()
